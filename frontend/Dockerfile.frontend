FROM node:20-alpine

WORKDIR /frontend

COPY package.json ./
COPY yarn.lock ./

RUN yarn install && \
    yarn cache clean

COPY . .

EXPOSE 8080

RUN yarn build

CMD ["node", ".output/server/index.mjs"]
